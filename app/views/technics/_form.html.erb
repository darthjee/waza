<%= form_for(@technic) do |f| %>
  <% if @technic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@technic.errors.count, "error") %> prohibited this technic from being saved:</h2>

      <ul>
      <% @technic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.text_field :category_id %>
  </div>
  <div class="field">
    <%= f.label :fase_id %><br>
    <%= f.text_field :fase_id %>
  </div>
  <div class="field">
    <%= f.label :meaning %><br>
    <%= f.text_field :meaning %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
